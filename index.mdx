---
title: "Introduction"
description: "Welcome to the docs of hakuna webhook engine docs"
mode: "wide"
---

```typescript
// Declaring a variable with a specific type
let message: string = "Hello TypeScript!";
console.log(message);

// Function with type-annotated parameters and return type
function add(a: number, b: number): number {
    return a + b;
}
let sum = add(5, 3);
console.log(sum);
```